#!/bin/bash
#Include enviroment variables
. $(cd `dirname "${BASH_SOURCE[0]}"` && pwd)/subutai.env

IP=""
while [ "$IP" == "" ]; do
	IP=$(echo $(ifconfig wan | grep 'inet addr' | awk -F: '{print $2}' | awk '{print $1}'))
	sleep 1
done

dnsmasq -k --address=/gw.intra.lan/$IP
