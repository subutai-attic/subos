#!/bin/bash
#Include enviroment variables
. $(cd `dirname "${BASH_SOURCE[0]}"` && pwd)/subutai.env

while [ "`ifconfig | grep '10.10.0.254' | wc -l`" -eq "0" ]; do
        sleep 1
done

dnsmasq -k --address=/gw.intra.lan/10.10.10.1 
